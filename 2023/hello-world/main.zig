const std = @import("std");

// This is more or less a scratchpad for learning/using
// Zig during advent of code.
//
// Run it with "zig run main.zig"
// Run the test file with "zig test test.zig"

pub fn main() void {
    std.debug.print("Hello, {s}!\n", .{"World"});
}
